# ============================================
# MINIMAL .ENV FOR EMAIL OTP TESTING
# ============================================
# Copy this content to backend/.env to get started quickly

# Server Configuration
NODE_ENV=development
PORT=8080

# Database - Choose ONE option below:
# Option 1: Local MongoDB (if you have it installed)
MONGODB_URI=mongodb://localhost:5173/jobportal

# Option 2: MongoDB Atlas (recommended - free cloud database)
# Sign up at: https://cloud.mongodb.com
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/jobportal

# JWT Secret (change this to any random string)
JWT_SECRET=your_random_secret_key_here_make_it_very_long_12345

# Email Settings (OPTIONAL - leave empty for development)
# In development mode, OTP will be printed to console
# No need to configure SMTP for testing!
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Optional Settings
FROM_NAME=Job Portal
FROM_EMAIL=noreply@jobportal.com
FRONTEND_URL=http://localhost:5173
